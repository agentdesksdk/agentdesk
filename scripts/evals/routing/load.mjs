// Stub. See schema.mjs.
export function loadRoutingTasks(path, specs, options = {}) {
  void path;
  void specs;
  void options;
  return [];
}

export function loadRoutingRecords(path, options = {}) {
  void path;
  void options;
  return [];
}
